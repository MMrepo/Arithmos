settings:
  groups: 
    - codeSign
settingGroups:
  codeSign: 
    CODE_SIGN_IDENTITY: $(inherited)
    CODE_SIGN_STYLE: Automatic
    IPHONEOS_DEPLOYMENT_TARGET: 10.3
    PRODUCT_BUNDLE_IDENTIFIER: com.vindur.Arithmos
    PRODUCT_NAME: $(TARGET_NAME:c99extidentifier)
    SWIFT_VERSION: 4.0
  testsTargetRunPathSearch:
    LD_RUNPATH_SEARCH_PATHS: "@executable_path/Frameworks @loader_path/Frameworks $(FRAMEWORK_SEARCH_PATHS) $(inherited)"